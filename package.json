{
  "name": "card-program-platform",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter @card-platform/* dev",
    "codegen": "graphql-codegen --config codegen.ts"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^6.1.3",
    "@graphql-codegen/typescript-resolvers": "^5.1.7",
    "tsx": "^4.7.0",
    "typescript": "^5.9.3"
  }
}